{
  "name": "shopee-return-manager",
  "version": "1.0.0",
  "private": true,
  "description": "Quản lý đơn hoàn huỷ Shopee trên Windows",
  "main": "src/main.cjs",
  "scripts": {
    "start": "electron .",
    "test": "node --test tests/*.test.cjs",
    "test:ui": "node tests/ui-smoke.cjs",
    "check": "node --check src/main.cjs && node --check src/core.cjs && node --check src/scanner.cjs && node --check src/integrations.cjs && node --check src/renderer.js"
  },
  "dependencies": {"playwright": "^1.55.0"},
  "devDependencies": {"electron": "^40.0.0"}
}
